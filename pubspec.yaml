name: flutter_qiita_client
description: A simple Qiita client application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_dotenv: ^5.0.0
  get_it: ^7.1.4
  provider: ^5.0.0
  rxdart: ^0.27.1
  intl: ^0.17.0
  url_launcher: ^6.0.6
  http: ^0.13.3
  freezed_annotation: ^0.14.2

dev_dependencies:
  test: ^1.17.8
  build_runner: ^2.0.4
  freezed: ^0.14.2

flutter:
  uses-material-design: true
  assets:
    - .env
